# react-helmet-async@2.0.5 declares peer react@"^16 || ^17 || ^18" but works
# fine with React 19 at runtime. This flag tells npm to resolve peer deps
# using the older (pre-v7) algorithm which warns instead of erroring.
# Required for Vercel's fresh `npm install` to succeed.
legacy-peer-deps=true
